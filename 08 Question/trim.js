// Demonstrate the use of trim() to remove any trailing whitespace at the beginning and the end of a string.

const strngSpaces = "   this is a string with spaces at the start as well as at the end    ";
console.log(strngSpaces);

// trim use
const strngTrimmed = strngSpaces.trim();
console.log(strngTrimmed);